image: registry.gitlab.com/pages/hugo:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:
  script:
  - hugo
  except:
  - master
  - replace_flask_with_hugo

pages:
  script:
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
  - replace_flask_with_hugo
